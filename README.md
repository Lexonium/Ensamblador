# Ensamblador-Cheang
Proyecto de Compiladores para recrear un ensamblador con mi equipo.

 *
 * Programa que detecte que esta leyendo(etiqueta, comando, variable, constante etc) [YA ESTA A MEDIAS TODAVIA NO DIFERENCIAMOS ENTRE VARIABLE Y CONSTANTE]

* si es un comando/etiqueta ir llevando el contador de tamaño de codigo para tener su direccion ya que se deberia guardar con direccion
 * si es variable/constante ir llevando el contador de tamaño de datos para saber su direccion tambien
 * si se refiere a una etiqueta sin haber definido antes esa etiqueta mantenerla en una tabla y revisar que si se define en algun momento
 * asegurar que siga lo correcto de un comando si es un DEFAI no poner nomas una variable
 * despues ponerlo junto para calcular el peso? Header-Datos-Codigo, cuadro de cheang?
 *
 *
 * =================================================================
 * Detalles 9/11/2021
 * NECESITAMOS SABER QUE PEDO CON LOS CONTADORES(SEGMENTO CODIGO, DATOS, DIRECCIONES)
 * VER SI PODEMOS ARREGLAR LO DE LOS ESPACIOS EN BLANCO DE LOS COMENTARIOS
 * IDENTIFICAR TIPO DE VARIABLE/SI ES UNA CONSTANTE
 * SE ESTAN GUARDANDO OTRA VEZ LAS VARIABLES(VARIABLES REPETIDAS)
 *
 *

